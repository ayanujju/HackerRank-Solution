#include <stdio.h>
#include <stdlib.h>
int main()
{
    int a[1000], num, i;
    scanf("%d", &num);
    for(i = 1; i <= num; i++) 
        scanf("%d", &a[i]);
    for(i = num; i >=1; i--)
        printf("%d ", a[i]);
    return 0;
}  
