#include <stdio.h>

int main() {
    int n,x,sum=0;
    scanf("%d", &n);
    if((n>=10000)&&(n<=99999))
    {
        while(n>0)
        {
            x=n%10;
            n/=10;
            sum+=x;
        }
        printf("%d",sum);
    }
    
    return 0;
}
