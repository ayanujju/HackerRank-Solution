#include<stdio.h>

int main()
{
    int t,n,i,v;
    scanf("%d",&t);
    scanf("%d",&n);
    int a[n];
    for(i=0;i<n;i++){
        scanf("%d ",&a[i]);
        if(t==a[i])
            v=i;
    }
    printf("%d",v);
}
