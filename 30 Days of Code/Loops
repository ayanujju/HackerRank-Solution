#include<stdio.h>

int main()
{
    int n,i;
    scanf("%d",&n);
    if(n>=2&&n<=20)
    {
        for(i=1;i<=10;i++)
        {
            printf("%d x %d = %d\n",n,i,n*i);
        }
    }
    return 0;
}
